/*
 * Find the maximum element in a heap
 */

#include <stdio.h>
int FindMax(int A[])
{
return A[1];
// root of max heap
}
int main()
{
int H[] = {0, 40, 30, 35, 10, 5, 20};
printf("Max element = %d", FindMax(H));
return 0;
}
